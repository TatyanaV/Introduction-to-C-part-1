#include <iostream>
#include <string> // gives you sting funcition
using namespace std;

int main () {
    string my_str;
    cin >> my_str;

        // loop that prints the string one character at a time
        for (int i = 0; i < my_str.length(); i++)
        {
            cout << my_str.at(i) <<endl;
        }


        for (int i = (my_str.length()); i= 0; i--)
        {
            cout << my.str.at(i) << endl;
        }



}
